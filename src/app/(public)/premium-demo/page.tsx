import { PremiumShowcase } from "@/components/ui/premium-showcase";

export default function PremiumDemoPage() {
  return <PremiumShowcase />;
}

export const metadata = {
  title: "Premium UI Demo - Good Chat",
  description: "Experience the new premium design system with enhanced colors, smooth animations, and beautiful glass morphism effects.",
};